## 2024-05-24 - Form Field Accessibility and Localization
**Learning:** Relying solely on HTML5 `required` attributes lacks visual clarity for cognitive accessibility, and default Bootstrap ARIA labels (like "Close") need localization for non-English apps.
**Action:** Always complement `required` attributes with visual asterisks (`aria-hidden="true"`) and translate ARIA labels to match the application's primary language.
