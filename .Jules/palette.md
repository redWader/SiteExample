## 2024-05-16 - Visual Indicators for Required Form Fields
**Learning:** Adding the HTML `required` attribute enforces validation but is invisible to sighted users before submission. This creates friction if users miss a field and only find out upon clicking submit.
**Action:** Always pair `required` attributes with visual indicators (like an asterisk) and helpful placeholders to guide users clearly before they interact with the form.
